apiVersion: v1
kind: Service
metadata:
  name: {{ include "openshift-ai-agent.fullname" . }}
  labels:
    {{- include "openshift-ai-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openshift-ai-agent.selectorLabels" . | nindent 4 }}
